import React from 'react'

import classes from "./NavigationBar.module.css"
const NavigationBar = () => {
  return (
    <header>
      <h1 className={classes.logo}>Tasked</h1>
    </header>
  )
}

export default NavigationBar
